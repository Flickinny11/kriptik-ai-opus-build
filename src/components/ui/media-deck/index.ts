/**
 * MediaDeck - V-JEPA 2 Visual Timeline Component
 *
 * Premium glass morphism component for visualizing temporal predictions,
 * state transitions, and visual semantic analysis.
 */

export { MediaDeck, default } from './MediaDeck';

// Re-export types
export type {
  TimelineFrame,
  StateTransition,
  MediaDeckProps,
} from './MediaDeck';
